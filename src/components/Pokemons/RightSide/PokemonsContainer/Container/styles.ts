import { styled } from "styled-components";

export const PokemonsCardContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  justify-content: start;
  align-items: start;
  max-width: 109rem;
  gap: 3.2rem;
`